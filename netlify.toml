[build]
  command = "npm run build"
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "20"

# Cấu hình Single Page Application (SPA) cho React Vite
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
